# SPE API - Postgres configuration
# Choose one option below and adjust credentials to your environment.
NODE_ENV=production
# API server port
PORT=3001

# Option 1: PG variables
# PGHOST=localhost
# PGPORT=5432
# PGUSER=postgres
# PGPASSWORD=password
# PGDATABASE=postgres
PGSCHEMA=spe

# Option 2: Single connection string (uncomment and adjust if preferred)
DATABASE_URL=postgres://postgres:password@localhost:5432/postgres